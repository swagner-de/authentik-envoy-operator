{{- if and .Values.certManager.enabled .Values.metrics.enabled .Values.metrics.secure }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "authentik-envoy-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "authentik-envoy-operator.resourceName" (dict "suffix" "metrics-certs" "context" $) }}
  namespace: {{ .Release.Namespace }}
spec:
  dnsNames:
  - {{ include "authentik-envoy-operator.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ include "authentik-envoy-operator.namespaceName" $ }}.svc
  - {{ include "authentik-envoy-operator.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ include "authentik-envoy-operator.namespaceName" $ }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: {{ include "authentik-envoy-operator.resourceName" (dict "suffix" "selfsigned-issuer" "context" $) }}
  secretName: metrics-server-cert
{{- end }}
